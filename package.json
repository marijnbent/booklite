{
  "name": "booklite",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "clean": "npm run clean -ws --if-present",
    "dev": "concurrently -n api,web -c cyan,magenta \"npm:dev -w @booklite/api\" \"npm:dev -w @booklite/web\"",
    "build": "npm run build -ws --if-present",
    "test": "npm run test -ws --if-present",
    "typecheck": "npm run typecheck -ws --if-present",
    "docker:up": "docker compose -f deploy/compose.yml up -d --build",
    "docker:down": "docker compose -f deploy/compose.yml down",
    "docker:dev": "docker compose -f deploy/compose.dev.yml up"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=22"
  }
}
